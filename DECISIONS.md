# DECISIONS.md

記載のない事項を自分で決めた記録。1行1決定。

- 2026-08-27: ステータスは HP / ATK / DEF / AGI の4種+役割で開始(REQUIREMENTS未決事項。+αはM2以降で必要になったら追加)
- 2026-08-27: 乱数は SHA-256(seed, counter) の counter-based 方式(fast-forward不要で決定的、セーブに seed と counter を記録)
- 2026-08-27: ダメージ式は max(1, round((ATK×倍率 − DEF×def_coeff) × 変動)) 、変動は±10%(係数は balance.json)
- 2026-08-27: 奥義ゲージは 通常攻撃+25 / アビリティ+15 / 待機+30 / 被弾+10、100で解放・使用で0(balance.jsonで調整可)
- 2026-08-27: ヘイトは 与ダメ×1.0 / 回復量×1.5 / バフ+5、挑発は「全員の最大ヘイト×2+50」に設定+ロック付与
- 2026-08-27: 挑発ロックは2ターン。ロック中のルール層敵は必ずタンクを対象にする(ボスの早期解除はM2以降)
- 2026-08-27: M1の敵はルール層のみ(AI呼び出しゼロ)。3の倍数ターンは強撃(×1.5)を使う固定パターンで脅威を作る
- 2026-08-27: 不正手(CT中アビ・ゲージ不足の奥義・存在しない敵対象)はエラー返信+Issueクローズ+ターン不消費(セーブ無変更)
- 2026-08-27: 戦闘が終了済み(勝利/敗北)または未開始の状態でターンIssueが来たら、新しい戦闘を自動開始してそのターンを解決する
- 2026-08-27: 対象「自動」の解決: 攻撃=敵1(生存する先頭の敵)、回復=HP割合最小の味方、バフ=効果定義のデフォルト対象
- 2026-08-27: 初期セーブはリポジトリにコミットして同梱(リセットは python -m engine.cli --reset)
- 2026-08-27: README反映は2コミット方式(①save+board.svg→②そのSHAを付けたREADME)で1回のpushにまとめ、キャッシュ回避を成立させる
- 2026-08-27: フォームの自由記述欄はM1では未対応(記入があれば「M2で対応予定」を返信に明記。スロット選択はそのまま実行)
- 2026-08-27: Issue Formのドロップダウンは required+default 付きで空選択を排除
- 2026-08-27: M1のセーブは save/state.json の単一ファイル(schema_version=1)。M2でスキーマ確定時に party/ spells/ へ分割
- 2026-08-27: 開発ブランチは claude/kickoff-prompt-autonomous-oumsyx(ハーネス指定)。issues起動ワークフローはデフォルトブランチのみで動くため、実機E2Eはmainマージ後に実施
- 2026-08-27: 仮パーティ名(世界データ側): ソラ(アタッカー)/リュノ(サポート)/ガンテ(タンク)/ミオ(ヒーラー)、M1固定敵は「星蝕の仔狼」
- 2026-08-27: ローカルはPython 3.11のため3.11互換で書く(Actionsは3.12)
- 2026-08-27: ローカル生成(sha=local)時のREADMEボードURLは相対パス(assets/board.svg)にフォールバック
- 2026-08-27: [レビュー反映] ボード画像は相対URL+`?v=t{ターン}-i{Issue番号}`クエリで参照(非公開リポジトリで絶対raw URLはcamo経由404になるため。SHA非依存でキャッシュ回避)
- 2026-08-27: [レビュー反映] README反映は1コミット方式に変更(2コミット+SHA埋め込みを廃止)。push拒否時はrebaseせず fetch+reset --hard でリモート先端に戻しターン全体を再解決(最大3回、冪等)
- 2026-08-27: [レビュー反映] turn.yml の checkout は ref=デフォルトブランチ(issuesイベントの github.sha はIssue作成時点で凍結され、直列化待ちのrunが古いセーブを読むため)
- 2026-08-27: [レビュー反映] ランナーはイベントのIssueに加えオープンな[TURN] Issueを番号順に全処理(concurrencyのpendingスロットは1つで、連投時にキャンセルされたIssueを次のrunが回収する)
- 2026-08-27: [レビュー反映] issue_parser は既知ラベルの見出しのみ区切り・重複は初出優先・自由記述見出し以降は全て自由記述(自由記述欄からの###見出しインジェクション対策)
- 2026-08-27: [レビュー反映] 挑発保持者が戦闘不能になったらロックを即時解除しログに明示(敵AI・ボード表示・セーブの状態を一致させる)
- 2026-08-27: [レビュー反映] 戦闘ログの技種別名は world.json の power_system.ability_term を使用(エンジンから固有名詞「星技」を排除。bot名も中立の rpg-engine[bot] へ)
- 2026-08-27: [レビュー反映] 通常攻撃倍率(damage.normal_attack_power)と回復下限(heal.min_heal)を balance.json へ一元化
- 2026-08-27: 奥義ゲージは戦闘間で持ち越す(明文化。start_battle はHP/CT/バフ/ヘイトのみリセット)
- 2026-08-28: M1実機E2E完了(8ターンで勝利・不正手不消費・連投健全)。v0.1-m1タグ付与。M2の実機AI受入には CLAUDE_CODE_OAUTH_TOKEN のSecrets設定が必要(ユーザー作業)
- 2026-08-28: [M2] レベルはパーティ共有1本(適正Lv概念と予算式を単純化)。勝利でXP獲得、レベルアップ毎に技生成権(spell_tokens)+1
- 2026-08-28: [M2] 技予算 = (base + per_level×(Lv-1)) × role係数。効果コスト表・CT割引(1-0.06×ct)・奥義は別枠(×3.0)を balance.json に定義。M1の固定16技はLv1予算に収まるよう係数を較正(フェアネスの基準)
- 2026-08-28: [M2] 効果タグ追加: debuff/stun(ボス耐性段階制: 同一CC毎に効果ターン-1)/dot(詠唱時スナップショット・ターン終了時に発火)/shield(被ダメを先に吸収)/scan(敵ステ・ヘイト表を開示しボードに表示)/dispel(敵バフ解除)。ヘイト操作は負値も許可
- 2026-08-28: [M2] セーブスキーマv2: state.json(乱数・戦闘・処理済みIssue・統計)/player.json(レベル・XP・生成権・ロスター)/party/1人1ファイル(スロット=技ID参照+CT状態)/spells/1技1ファイル(定義・使用回数・撃破数・更新履歴)/enemies/(生成敵)/log.md(旅の記憶=AIプロンプト同梱用)。v1からの自動移行を実装
- 2026-08-28: [M2] 予算カーブ・enemy_scaleも balance.json に置く(「バランス係数はbalance.jsonに一元化」を優先。world.jsonは名称・世界観・図鑑のみ)
- 2026-08-28: [M2] AIモデルconfigは config/ai.json: turn=claude-haiku-4-5-20251001(軽量)/ generation=claude-sonnet-5(上位)。claude CLI の --model に渡す
- 2026-08-28: [M2] タグpushはセッション認証で不可のため .github/workflows/tag.yml(workflow_dispatch)でGITHUB_TOKENが打つ
- 2026-08-28: [M2レビュー反映] 敵アクションの効果スキーマをタグ毎のoneOf(必須フィールド+buff/debuffのmult範囲分離)にし、予算合算は効果毎に0未満をクランプ(負コスト相殺の悪用防止)。検証中の想定外例外も全てフォールバック
- 2026-08-28: [M2レビュー反映] CTコストは割引でなく頻度係数 (ct_reference/ct)^ct_exponent に変更(CT1は割増)。アビリティのCT0は禁止。初期16技がLv1予算内に収まることは維持
- 2026-08-28: [M2レビュー反映] 知能層の制約強化: 非知能敵のIDはAI応答から無視/特殊技はルール層と同じ周期でしか使えない(last_special_turnで管理)
- 2026-08-28: [M2レビュー反映] pending_update に対象技IDを記録し、生成でスロットが変わったら提案を無効化。選択適用時にも予算を最終検証
- 2026-08-28: [M2レビュー反映] claude CLI 呼び出しは --max-turns 1+全ツール禁止+最小環境変数(GITHUB_TOKEN等は渡さない)。応答の is_error を検査。フォームの自由記述は500文字に制限
- 2026-08-28: [M2レビュー反映] AIタイムアウト60秒・ジョブ20分(最悪ケース3呼び出し×3試行が収まる)。npm install失敗はルール層続行
- 2026-08-28: [M2レビュー反映] レベルアップは戦闘不能者を蘇生しない(成長のみ)。フォールバック3案は全ノブ縮小+最終的に現行技へ退避で予算内を保証
- 2026-08-28: 同時全滅(相打ち)は勝利として扱う(_check_endの判定順。最後の一撃が通った側の勝ち)
- 2026-08-28: [M2] 技生成の却下時は却下理由をプロンプトに添えて再生成(最大3試行)→それでも駄目ならフォールバック(仕様「却下時は再生成」の充足)。フォールバック名は読点前で切り出す
- 2026-08-28: [M3] 素材パイプライン: assets/raw/ の緑背景画像→assets/parts/ にWebP+manifest.json。命名規則: background*/bg* =背景、part*/wing* =可動パーツ、それ以外=胴体。連結成分は大きい順最大2個をパーツ採用
- 2026-08-28: [M3] クロマキー判定: G > R×1.25 かつ G > B×1.25 かつ G > 90。エッジは緑にじみ除去(G成分をmax(R,B)へクランプ)
- 2026-08-28: [M3] サイズ予算: シーン合計(base64後)上限1MB・目標500KB。品質[88,80,70,60]×縮尺[1.0,0.85,0.7,0.55]の梯子で自動段階ダウン。素材解像度の下限=表示サイズ×1.0
- 2026-08-28: [M3] 可動パーツのpivotは「胴体中心に最も近いパーツ端点」のヒューリスティック(manifest.jsonを手で直せば上書き可能)
- 2026-08-28: [M3] シーンSVGは戦闘開始時のみ生成(素材があれば内包合成、なければ手続き的プレースホルダ=星空+ティア別シルエット)。SMILで登場・待機・羽ばたき・星の瞬き。ボードSVGは毎ターン+直近ターンのログをSMILで順次表示(ターンリプレイ)
- 2026-08-28: [M3] Gemini接続は境界モジュール(REST・GEMINI_API_KEY必須・失敗はプレースホルダ続行)。敵の新規生成時に素材3枚(背景/胴体/可動)を自動生成し assets/raw/ に置いてパイプラインへ
- 2026-08-28: [M3] assets/raw/ へのpushで assets.yml が起動しパイプライン実行→parts/シーン/READMEをコミット(受入の「置くと自動で合成」)
- 2026-08-28: [M3レビュー反映] cli.py は GITHUB_REPOSITORY / GITHUB_SHA を既定値に使う(assets.ymlがOWNER/REPOの死にリンクREADMEをコミットする事故の修正)。assets.ymlも--repoを明示
- 2026-08-28: [M3レビュー反映] Gemini生成素材は assets/raw/.generated.json に敵IDを記録し、敵が変わったら作り直す。マーカーの無い素材=ユーザーの手置きとして決して上書きしない。push競合リプレイ中(attempt>0)は再生成しない
- 2026-08-28: [M3レビュー反映] シーン生成に失敗したら古い scene.svg を削除(前の敵のシーンを誤表示しない)。parts/の孤児WebPも毎回掃除。EXIF回転を正規化。素材予算の上限は1MB-64KB(マークアップ余白)。背景画像がある時は手続き背景を省略
- 2026-08-28: [M3レビュー反映] パーツは胴体と一括スケールし、はみ出す場合は敵の中心を左に寄せて全体を枠内に収める
- 2026-08-28: [M3] assets.yml と turn.yml の concurrency 共有は維持(セーブ/画面の競合防止を優先)。稀にpending runが落ちるが、次のIssueイベントのオープンIssue一括処理が回収する(許容トレードオフ)
- 2026-08-28: [M3] テストは GEMINI_API_KEY / CLAUDE_CODE_OAUTH_TOKEN を常に外して実行(autouse fixture)。Geminiキーはヘッダ送信(URLに載せない)
- 2026-08-28: [M4] 盤面残留タグ=効果タグ「field」(名前はworld.jsonのfield_tags辞書から)。チェインはworld.jsonのchain_reactions(必要タグ+着弾タグ→倍率・消費・ログ)で、damage効果に"field"を添えた攻撃が反応をトリガーする。敵も使用可
- 2026-08-28: [M4] 属性システムはv1では実装しない(チェインは残留タグで表現。5属性相性はworld.jsonに定義だけ残し将来拡張)
- 2026-08-28: [M4] 制約タグ辞書(エンジンが機械判定可能なもののみ): hp_below_30 / self_stun_after / once_per_battle / first_three_turns / vs_elite_plus。予算乗算は合計×3でキャップ。違反は不正手(ターン不消費)
- 2026-08-28: [M4] 適応進化トリガー: HP50%割れ(1回)/同一CC2回目。上限は standard以下0・elite1・boss2。発動は必ず1ターン前に予告→次ターン冒頭で進化(AI生成・特殊技予算×進化ボーナス検証・失敗はルール層変異)。歪み=弱点フィールドタグをエンジンが強制付与(該当タグを添えた攻撃は倍率増)。スキャンで兆候・弱点とも開示
- 2026-08-28: [M4] 歴史の共鳴: 装備中の「最古の技(初期技=世代0、生成技=_genN)」と「最新世代の技」を同一ターンに使うと発動(1戦闘1回)。最古の技のコストを現行予算水準まで引き上げる倍率(上限×3)を適用。初発動までREADME/フォームでは非公開、発動後はlog.mdに記録
- 2026-08-28: [M4] フルオートN: ターンフォームの自由記述「フルオート N」(N≤8)で発動。方針はルール層(強い技優先・挑発ローテ・HP70%未満で回復)でNターン連続処理し、要約ログを返信。戦闘終了で打ち切り
- 2026-08-28: [M4] 宿敵: 敗北時の敵を進化・弱点・記憶ごと save/enemies/ に保存し、次の戦闘で必ず再登場(HP全快)。撃破で解消。log.mdに「一度死んだ」記憶を記録しAIプロンプトへ
- 2026-08-28: [M4] 時戻し: [REWIND]フォーム。コスト=技生成権1。git履歴から現在の戦闘の開始時点のコミットを特定し save/ を復元、時戻しの事実をlog.mdに追記(履歴改変はしない)
- 2026-08-28: [M4] PR攻撃: boss層のみ・1戦闘1回・HP60%割れで詠唱開始。実PR([Boss Attack])を作成し battle_override.json(バランス係数の戦闘スコープ上書き)を提示。制限3ターン内に「閾値ダメージでブレイク」または「PRを手動クローズ」できなければ強制マージ。戦闘終了時にoverrideを撤去。turn.ymlに pull-requests: write を追加(M4指定どおり)
- 2026-08-28: [M4-A実装] self_stun_after の代償は stunned_turns=N+1 で積む(使用ターン終了時の減算を相殺し、確実に次のNターンを失わせる)
- 2026-08-28: [M4-A実装] 進化の演出生成(generate_evolution)は敵生成・勧誘と同じ「生成イベント」扱いの別呼び出し(ターン同梱1回の制約はターン処理=敵AI判断+実況にのみ適用、の既存整理を踏襲)。進化技の予算=特殊技予算×evolution.action_budget_mult(1.3)、超過や不通は決定的フォールバック演出
- 2026-08-28: [M4-A実装] 共鳴の増幅率=初代技コストに対する現在予算の比(1〜resonance.amp_cap=3にクランプ)。同ターンの共鳴2技の両方に同率適用(damage/healに乗算)。検出は宣言時、消費(resonance_used)は最初の共鳴技が実際に発動した瞬間
- 2026-08-28: [M4-A実装] damage効果の添えタグ("field"キー)のコスト=field.cost_per_turn×2(チェイン不成立時に2ターン付与相当のため)。fieldは攻撃系タグ扱い(対象=敵のみ)。残留タグは対象ごと最大4スタック・飽和時は弾く
- 2026-08-28: [M4-A実装] フルオートNは投稿ターンを含む合計Nターン(上限balance.full_auto_max_turns=8)。自動採択は 奥義解放>回復(誰かHP60%未満)>攻撃アビ>通常攻撃 の決定的ルール。誓約付きの技は条件外で不正手になり得るため自動では使わない
- 2026-08-28: [M4-A実装] 誓約checkboxはフォーム文言と balance.constraints[].label の先頭一致でIDへ解決(未知の文言は無視)。誓約は技アップデートで引き継がれ、更新予算も乗算されたまま。制約の閾値(HP比率・ターン数・対象tier・スタン数)も balance.constraints の各エントリに一元化
- 2026-08-28: [M4-A実装] 進化の数値はスクリプト固定: atk×evolution.bonus_mult、歪み弱点はセーブ済み乱数で distortion_weaknesses から未付与の1つを選択(倍率=weakness_mult)。AIは名前・セリフ・進化技の見た目のみ。CC契機進化は進化履歴のreasonで1回制限
- 2026-08-28: [M4-B実装] 宿敵は Save.nemesis(state.json)に敵の全状態(進化・歪み・進化技込み)で保存。再戦時は一時状態(バフ/DoT/CC耐性/残留タグ)だけ初期化しHP全快、hp_evolution_triggered等の記憶は引き継ぐ。宿敵がいる間は敵生成をスキップして必ず再登場、宿敵以外を倒しても解消しない
- 2026-08-28: [M4-B実装] 誓約・battle_uses のv2セーブ往復を修正(spells/*.json に constraints、slot_state に battle_uses、ultimateのslot_stateを新設)
- 2026-08-28: [M4-B実装] 時戻しの対象=「現在の戦闘(名前一致・active)が載る履歴上最古のコミット」。戦闘開始そのものの瞬間はコミットされない(開始+ターン1解決が1コミット)ため、戻り先は実質ターン1解決直後。既に最古ならエラーで生成権不消費
- 2026-08-28: [M4-B実装] 時戻しはワークツリーをgit checkoutせず、対象コミットのsave/を一時ディレクトリに展開して読み(失敗時セーブ不変)、通常のwrite_save+コミットとして積む(履歴改変なし)。processed_issuesは現在と過去の和集合(巻き戻しで処理済みIssueを未処理に戻さない)。乱数も巻き戻るため同じ手は同じ結末(リプレイ原則を維持。異なる選択で分岐)
- 2026-08-28: [M4-B実装] 時戻しの代償は復元後のセーブから技生成権1(戦闘中に生成権は増減しないため現在値と一致する)。魔導書(spells/)の後発ファイルはgrimoire仕様のためそのまま残す
- 2026-08-28: [M4-C実装] PR攻撃の状態機械はbattle.py(純粋): boss層HP60%割れで pending(予告ログ)→ runnerが実PR作成で casting(期限=作成ターン+3-1)→ 詠唱ボスへの直接ダメージ合計90で broken / 期限到達で deadline。実PRの作成・監視・強制マージ・クローズは turn_runner._process_pr_attack(I/O境界)が担い、gh不在時はシミュレートで状態だけ進める(ゲームを止めない)
- 2026-08-28: [M4-C実装] battle_override.json の中身はAIではなく balance.pr_attack.override(def_coeff 0.1=防御が意味を失う+回復弱体)。適用は turn_runner が balance に深マージ(リプレイ毎に再読込。push競合の同期でファイルが現れても正しく反映)。戦闘終了時にファイル撤去+残PRクローズ+ブランチ削除
- 2026-08-28: [M4-C実装] 強制マージはターン処理のpushより先に行われるため必ず1回push競合→リプレイで override 適用後の状態に収束する(既存のリプレイ機構を再利用)。マージAPI失敗時も「放置された」事実は変わらないため効果は発動(ローカル書込+PRクローズ)
- 2026-08-28: [M4-C実装] boss層の出現周期を追加: boss_every_battles=8(8戦目ごと)。elite(4戦目ごと)より優先。PR攻撃のブレイク判定は詠唱ボスへの直接ダメージのみ(DoTは含めない)
- 2026-08-28: [M4レビュー反映] 時戻しの代償は「復元後のセーブ」ではなく「現在のセーブ」の生成権から引く(復元値から引くと同じ地点へ何度戻っても合計1トークンで済む無限リワインドになる)
- 2026-08-28: [M4レビュー反映] 誓約の発動条件は validate_commands(ターン開始)に加えて発動の瞬間にも再検証する(解決中の回復でHP条件を外れる/対象が倒れて自動再選択で格下の敵に向く経路を塞ぐ)。不発時はアビリティのCTのみ消費、奥義はゲージを温存
- 2026-08-28: [M4レビュー反映] 歴史の共鳴は宣言だけでは成立させない。発動時に相方が「既に放った」か「生存かつ行動可能」であることを確認する(相方が先に倒される/行動不能でも増幅が乗る穴を塞ぐ)
- 2026-08-28: [M4レビュー反映] 誓約IDはエンジン実装のあるものだけ有効(spells.ENGINE_CONSTRAINTS)。balance.jsonにIDを足しただけでは予算は拡張されない(代償なしの予算水増しを防ぐ)。同一誓約の重複指定も1回分のみ
- 2026-08-28: [M4レビュー反映] 時戻しはPR攻撃の状態を巻き戻さない(実PRとマージ済みのbattle_override.jsonは外の世界の出来事で戻せないため)。現在の状態を引き継ぎ、猶予ターンだけ戻した時間に張り直し、与ダメージ蓄積はリセットする(時戻しで削り直しを稼げない・PRが二重に作られない)
- 2026-08-28: [M4実機対応] フルオートの自動ターンでも知能層AIを使う(実機計測でAI応答は1ターン約20秒。敵の知能と実況はM4の面白さの中核なので落とさない)。上振れ対策は実時間の打ち切り(balance.full_auto_deadline_seconds=720秒で自動送りを停止し、続きは次のターン入力へ)とturn.ymlのtimeout 30分の二段構え
- 2026-08-28: [M4レビュー反映] 共鳴の相方判定は「技ID→(相方の技ID, 相方のメンバーID, 相方の対象)」で持つ(メンバーIDを技IDの集合と照合していて「相方は既に放った」が常に不成立だった)。相方が未行動なら生存・行動可能に加えて誓約条件も満たすことを要求する(相方が不発なのに増幅と1戦闘1回の消費だけ起きるのを防ぐ)
- 2026-08-28: [M4レビュー反映] 宿敵の再戦では hp_evolution_triggered をリセットする(進化履歴・歪みは記憶として残す)。総進化数は evolutions_used とティア上限で抑えるため、再戦で枠が残っていればHP契機の進化が再び起きる
- 2026-08-28: [M4レビュー反映] 歴史の共鳴の増幅は初代技(gen0)だけに乗せる。最新世代の技は既に現行予算いっぱいで作られており、同じ倍率を乗せると誓約で拡張した予算の上にさらに無料の×3が乗ってしまうため(実機で初代技の安さに比例して最新技が118ダメージに跳ねた)。最新技は「共鳴の片割れ」として成立条件には要るが増幅は受けない
- 2026-08-28: [M4レビュー反映] damageに添えたfieldタグのコストは「2ターン付与相当」に加えてチェイン増分(field.chain_mult_reference=1.8)を前払いさせる。従来は倍率が無料で、1つの技で仕込みと起爆を自己完結できた。ターン跨ぎの連携自体は禁止しない(味方同士の仕込み→起爆は狙って良い動き)ため、禁止ではなく価格で解決する
- 2026-08-28: [M4レビュー・許容] 誓約 self_stun_after はトドメを刺したターンには代償が実質発生しない(次のターンが来ないため)。戦闘を跨いで行動不能を持ち越すのは設計として悪いので、この非対称は許容する(once_per_battleやCTと併用すれば十分抑制される)
- 2026-08-28: [M4レビュー・許容] 共鳴の相方確認は発動時点の「生存・行動可能・誓約条件」までとする。判定後に相方が倒される可能性は残るが、先読みは行動順の決定性を壊すため追わない
- 2026-08-28: [M4レビュー反映] PR攻撃は「実PRの状態」を真実の源とする。強制マージは必ずpush拒否→リプレイを起こし、同じターンが歪んだルールで再解決されるため、盤面側の再計算だけを信じると「マージ済みなのにブレイク成立」という食い違いが生じる。casting/deadline/broken では毎回 get_pull で実状態を確認し、マージ済み→merged、クローズ済み→sealed に矯正する
- 2026-08-28: [M4レビュー反映] PR作成を冪等化(同名ブランチのオープンPRがあれば引き継ぐ・ブランチ存在チェック)。リプレイで2本目のPRが開かれ、1本目が迷子になるのを防ぐ
- 2026-08-28: [M4レビュー反映] 戦闘終了時の後始末はAPI成功時のみ終端状態にする(失敗時は次イベントで再試行。overrideを持つPRが開いたまま放置されない)
- 2026-08-28: [M4レビュー反映] フルオートはボスの禁忌詠唱が「始まった瞬間」(status=pending への遷移)だけ停止する。pr_attack は戦闘中Noneに戻らないため、存在判定にすると詠唱開始後のフルオートが毎回1ターンで止まってしまう
- 2026-08-28: [M4レビュー反映] フルオートの自動採択は回復要否をメンバー単位で解く(全体フラグだと誰か1人が傷ついた瞬間に、回復技を持たない者まで通常攻撃に落ちていた)
- 2026-08-28: [M4レビュー反映] フルオートの実時間打ち切りはプロセス起動時刻からの経過で測りジョブ全体を縛る(1ジョブ=複数Issue×最大3リプレイのため、_handle_turn毎に予算を張り直すと上限が効かない)
- 2026-08-28: [M4] 戦況ボードにM4の状態を表示する(残留タグと残ターン・進化の兆候/回数・誓約付き技の⛓マーク・禁忌詠唱の猶予と残り必要ダメージ)。ボードを見て次の手を決めるのが本作の中核ループであり、ログにしか出ない状態はターン間で見えず「実装したが遊べない」ため。歪み(弱点)はスキャン済みの敵のみ開示し、スキャンの価値を保つ
- 2026-08-28: [M4レビュー反映] 時戻しは pr_attack.break_need も捨てる(damage_since を0に戻すのに、表示用の残り必要ダメージだけ据え置くとボードが誤表示する)。ブレイク後のPRクローズもAPI成功時のみ終端状態にする
- 2026-08-28: [M4レビュー反映] 世界固有の語(通貨名・世界の理・進化フォールバックの名前・既定の残留タグ名)を world.json の system_terms へ移し、エンジンからは _term() 経由で引く。CLAUDE.mdの「エンジンコードに世界の固有名詞を書かない」不変則に対する違反だったため。world.jsonの値がengine/*.pyに現れたら落ちる回帰テストを追加
- 2026-08-28: [M4レビュー反映] battle_override.json は allow-list(damage/heal/hate/taunt/cc/enemy)で戦闘スコープの係数のみ許可する。leveling や spell_budget まで無条件に深マージできると、恒久的な進行をリポジトリ上のファイルで書き換えられてしまう
- 2026-08-28: [M4レビュー反映] フルオートの実時間打ち切りは廃止する。解決ターン数が実行環境の速さで変わるとセーブが非決定的になり、「リプレイ再現可能」の不変則を壊すため。上限は full_auto_max_turns(決定的)とジョブtimeoutの二段で担保し、超過時はpushしないまま次のイベントで再処理される
- 2026-08-28: [M4レビュー反映] フルオートの回復切替閾値・進化フォールバックの威力・添えタグの付与ターン数を balance.json へ移す(バランス係数の一元化)。PR攻撃の sealed 経路でもブランチを掃除する(同名ブランチが残ると次のPR攻撃が pending で固着する)
- 2026-08-28: [M4レビュー反映] PR攻撃のPRは採用時とマージ前に素性を検証する(作者がgithub-actions[bot]で、変更が battle_override.json だけ)。ブランチ名 boss-attack-<enemy_id>-t<N> は完全に予測可能で、エンジンは無審査でマージする権限を持つため、第三者が置いたPRを乗っ取らせない。素性が合わなければ別ブランチで作り直し、すり替えが疑われるPRはマージせず詠唱を不発(sealed)にする
- 2026-08-28: [M4レビュー反映] battle_override.json はセーブ側で pr_attack.status == "merged" が記録されている時だけ効かせる(ファイルが置かれているだけで戦闘バランスが変わってはいけない)
- 2026-08-28: [M4レビュー反映] Issueフォームの各値は120文字で切る(自由記述以外は無制限だったため、巨大な値が返信Markdownに連結されてコメントAPIが落ち、そのIssueが永久に処理できなくなり得た)
- 2026-08-28: [記録] 冒険の全文を `save/chronicle/chapter-NNN.md` に残す(年代記)。`log.md` は1行サマリの目次でAIプロンプトに同梱する用途、年代記は書籍化の素材なので要約せず上限も設けない。章=戦闘(stats.chapters)で、その戦いの全ターンと直後の拠点での出来事を時系列で1章にまとめる
- 2026-08-28: [記録] 年代記の書き込みはIssue番号をマーカーにした冪等な置換とする(push競合のリプレイで同じIssueが再解決されるため単純な追記だと本文が二重になる)。置換は位置を保ち時系列を崩さない。書き込み失敗は握りつぶして冒険を止めない(記録は大切だが進行を人質に取らない)
- 2026-08-28: [記録] 技生成の**詠唱文**と誓約を年代記に残す(技JSONには名前と効果しか無く、プレイヤー自身の言葉はどこにも保存されていなかった。書籍の核になる素材のため)
- 2026-08-28: [記録] 年代記の導入より前の冒険は、Issueの返信コメントから復元する(engine/backfill.py + chronicle.yml の workflow_dispatch)。エンジンの返信には全文が載っており、Issueは消えないため。復元した章には「Issueから復元した記録」と明記し、実際に記録された章は上書きしない(実記録の方が正確なため)
- 2026-08-28: [記録] 不正手の返信は復元対象から外す(ターンを消費しておらず、冒険の出来事ではないため)。ナビゲーションリンクも落とす(書籍にリンクは不要)
- 2026-08-28: [書籍化] 年代記を1冊へ編む [BOOK] フォームを追加。章ごとの語りは book/chapters/ にキャッシュし、素材のハッシュ(src-sha)が変わった章だけ編み直す。1回の実行で編む章数は balance.book.max_ai_chapters_per_run(8)で抑え、章が増えてもジョブ時間が伸びないようにする(続きは再実行で編める)
- 2026-08-28: [書籍化] 書物を編む行為は年代記に記録しない。記録するとその章自身が変わり、次の編纂で必ず編み直しになる自己参照ループが起きるため
- 2026-08-28: [書籍化] AIには「記録を改変しない・起きていない出来事を足さない」ことを課し、失敗した章は記録そのものを載せる(欠落を作らない)。編纂はセーブを一切変更しない読み取り専用の行為
