# 実装指示書: GitHub完結型AI生成RPG(ワールド1: アステリア)

あなた(Claude Code)への依頼: このリポジトリに REQUIREMENTS.md(v0.5) のゲームを実装する。マイルストーン M1→M2→M3→M4 を自律ループで進め、各受入基準を満たすまで「実装→検証→修正」を繰り返す。判断に迷ったら REQUIREMENTS.md を正とし、記載のない事項は自分で決めて DECISIONS.md に記録して進む。ユーザーへの質問は、ゲームの手触り(演出の方向性など)に関わる大きな分岐のみに限る。

## 最初にやること(順番厳守)

1. REQUIREMENTS.md を精読する
2. CLAUDE.md を作成し、本書の「不変の制約」節を転記する(以後の全セッションで常時読み込ませるため)
3. ARCHITECTURE.md を書く: リポジトリ構成、データフロー(Issue→Actions→engine→save/→README)、モジュール分割
4. PROGRESS.md を作成する: マイルストーンごとのチェックリスト。以後、毎コミットで更新する
5. M1 に着手する

## 自律ループの回し方

- サイクル: 小さく実装→ローカルテスト→コミット→push→実機検証→修正
- 戦闘エンジンは純粋関数+CLIとして設計し、Actionsなしでローカル実行できること: `python -m engine.cli --input fixtures/turn.json`
- AI呼び出しは必ずモック可能にする(`--mock` で fixtures/ の固定JSON応答を返す)。ユニットテストは全てモックで回す
- 実機検証は gh CLI で自動化する: `gh issue create` でテストターンを投稿→`gh run watch` で完了待ち→README・ボードSVG・返信コメントを取得して期待値と照合→不一致なら修正
- 各マイルストーン完了時: 全テスト+実機E2Eを通し、PROGRESS.md に完了記録、gitタグ(v0.1-m1 等)を打ち、簡潔な完了報告を出して次のマイルストーンへ進む
- コミットは小さく頻繁に。壊れた状態でマイルストーンをまたがない

## マイルストーンと受入基準

### M1: 最小戦闘ループ
- Issue Form(固定YAML・スロット語彙: アビ1/アビ2/アビ3/奥義/通常攻撃/待機 × 敵1〜3/各役割/自動)→Actions起動→固定データの敵1体との戦闘を素早さ順で解決→戦況ボードSVG(軽量・自己完結)をREADMEへ反映→勝敗判定→Issueをクローズ
- concurrency直列化 / Issue作成者==リポジトリオーナーのチェック / 不正手はエラー返信しターン不消費
- 仮パーティ4人(A/S/T/H)は固定データでよい。ヘイト・挑発ロック・CT・奥義ゲージはここで実装
- 受入: フォームから3ターン以上連続で戦い勝利できる。フォーム連投してもセーブが壊れない

### M2: 生成系
- 技生成(予算制・効果タグ辞書・jsonschema検証・却下時は再生成)、技アップデート(進化方向3案の提示と選択)、敵生成、敵AI二層(知能層は戦況JSON→コマンドJSONのみ)、レベルアップ、勧誘によるロスター追加、セーブスキーマ確定(player / party / spells / enemies / log)
- AI応答は全てJSON強制+スキーマ検証。失敗時はリトライ2回→ルール層フォールバック(ゲームを止めない)
- 受入: 技を生成→戦闘で使用→アップデート、の一連が実機で動く。敵AIがヒーラー狙いと挑発ロック遵守を実演する

### M3: 演出
- 素材パイプライン: assets/raw/ に置かれた緑背景画像をクロマキー(PIL+numpy)→パーツ分離(連結成分)→サイズ予算検査(シーン≤1MB目標500KB/ボード≤50KB)→超過時は解像度とWebP品質を自動段階ダウン
- シーンSVG(素材内包・戦闘開始時のみ生成)+ボードSVG(毎ターン)の2枚構成。ターンリプレイ演出(SMIL)。README画像URLはコミットSHA付きでキャッシュ回避
- 画像素材が無い間はプレースホルダSVG(シルエット)で全機能を成立させる。GEMINI_API_KEY があればGemini APIで素材自動生成まで接続する
- 受入: assets/raw/ に素材3枚を置くと自動で合成され、戦闘開始時に演出付きシーンがREADMEに表示される

### M4: ギミック
- 適応進化+歪み(予告演出・回数上限・弱点タグ強制付与)、スキャン、チェイン反応表(world.jsonデータ駆動)、制約と誓約(制約タグ辞書・上限×3)、宿敵、時戻し(セーブスナップショット復元+ログ追記)、PR攻撃(battle_override限定・制限ターン・強制マージ)、歴史の共鳴(1戦闘1回の隠しチェイン)、フルオートN
- 受入: 各ギミックの実機デモ手順を PROGRESS.md に記載し、実際に1回ずつ発動を確認する

## 不変の制約(CLAUDE.md へ転記すること)

- エンジンコードに世界の固有名詞を書かない。世界の個性は world.json のみが持つ
- AIの出力は必ずJSONで受け、スクリプト検証を通過したものだけ採用する。効果=効果タグ辞書へのコンパイル、入力の自由文=スロットへのマッピング。AIが戦闘結果や数値を直接決めることは決してない(数値の最終決定権は常にスクリプト=予算制)
- ターン処理のAI呼び出しは1ターン1回に同梱する(敵AI判断+ログ文+演出指示)。モデルはconfigで切替可能にする(ターン処理=軽量モデル、生成イベント=上位モデル)
- 認証はサブスクリプションのみ: 実行時AIは CLAUDE_CODE_OAUTH_TOKEN で認証する。ANTHROPIC_API_KEY はSecretsにも環境にも一切設定しない(存在するとAPI従量課金が優先されてしまうため)
- 乱数は全てシードをセーブに記録し、リプレイ再現可能にする
- READMEに載せるSVGは自己完結(外部リソース参照禁止)。シーン≤1MB、ボード≤50KB
- Secretsやその値をログへ出さない。AIレスポンス全文もログに吐かない(要約のみ)
- workflow: concurrency は group固定・cancel-in-progress: false。permissions は最小限(contents: write, issues: write。M4のPR攻撃時のみ pull-requests: write を追加)
- 基準戦闘: 適正Lvパーティが適正ボスに8ターン前後で勝利し残HP約4割。バランス係数は balance.json に一元化し、後から調整可能にする

## 技術スタック

- Python 3.12。依存は最小限: Pillow、numpy、jsonschema、pytest。scipy 使用可
- 実行時のAI呼び出しはAnthropic SDKではなく、Claude Code CLIのヘッドレスモードを使う: ワークフロー内で claude CLI をインストールし、環境変数 CLAUDE_CODE_OAUTH_TOKEN(サブスクリプション認証)の下で `claude -p "<プロンプト>" --output-format json` をサブプロセス実行して結果JSONを受け取る。エンジン側はこの呼び出しを1つの境界モジュールに隔離し、モック差し替え可能にする
- GitHub Actions は ubuntu-latest。デバッグ用に workflow_dispatch でも起動可能にする
- コード規約: 型ヒント必須、ロジックは純粋関数、I/O(git・GitHub API・AI API)は境界モジュールに隔離

## 進め方の作法

- 質問より決定+記録。決めた事項は DECISIONS.md に1行ずつ残す
- 各マイルストーン完了時、PROGRESS.md に「ユーザーがスマホで確認する手順」(開くURL、押すボタン、期待される表示)を必ず書く
- 完成の定義: M4受入基準まで全て満たし、ユーザーがスマホのGitHubアプリだけで1つのダンジョンを踏破できること
